{% extends 'user.html.twig' %}

{% block title %}
    Product
{% endblock %}

{% block body %}
    <div class="text-center">
        <h1>Add Product</h1>
    </div>
    {{ form_start(form) }}
    {{ form_row(form.product) }}
    {{ form_row(form.stockReel) }}
    {{ form_row(form.stockOpti) }}
    <div class="text-right">
        <button class="btn btn-primary">Save</button>
    </div>
    </div>
    {{ form_end(form) }}
{% endblock %}