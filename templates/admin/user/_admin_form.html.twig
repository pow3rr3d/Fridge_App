    <input type="hidden" name="_token" value="{{ csrf_token('update' ~ account.id) }}">
    <button class="btn btn-danger" data-toggle="modal"
            data-target="#modal_admin{{ account.id }}">Make Admin
    </button>