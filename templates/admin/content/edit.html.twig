{% extends 'admin.html.twig' %}

{% block title %}Edit Content{% endblock %}

{% block body %}
    <div class="text-center">
    <h1>Edit Content</h1>
    </div>
    <div class="text-right mt-4">
        <a href="{{ path('content_index') }}" class="btn btn-secondary">back to list</a>
    </div>
    {{ include('admin/content/_form.html.twig', {'button_label': 'Update'}) }}

{% endblock %}
